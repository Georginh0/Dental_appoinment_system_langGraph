# DentAI Pro — Production Requirements
# =====================================
# Install: pip install -r requirements.txt
#
# Local dev setup:
#   conda create -n dentai python=3.11 -y
#   conda activate dentai
#   pip install -r requirements.txt

# ── LangGraph / LangChain ──────────────────────────────────
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.9          # Groq integration (free LLM)

# ── Database — Supabase / PostgreSQL ───────────────────────
psycopg2-binary>=2.9.9         # PostgreSQL driver

# ── Web frameworks ─────────────────────────────────────────
fastapi>=0.111.0               # WhatsApp + Telegram webhooks
uvicorn[standard]>=0.29.0      # ASGI server
streamlit>=1.35.0              # Web UI

# ── Messaging integrations ─────────────────────────────────
twilio>=9.0.0                  # WhatsApp Business API
python-telegram-bot>=20.7      # Telegram Bot API

# ── Utilities ──────────────────────────────────────────────
python-dotenv>=1.0.0           # .env file loading
httpx>=0.27.0                  # Async HTTP (used by integrations)

# ── Development / testing (not needed on Render) ───────────
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# black>=24.0.0
# ruff>=0.4.0
